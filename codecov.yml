comment: true

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
        informational: true
    patch:
      default:
        target: auto
        threshold: 1%
        informational: true

fixes:
  - "::pkg/BayesFactor/" # move root e.g., "path/" => "pkg/BayesFactor/"
